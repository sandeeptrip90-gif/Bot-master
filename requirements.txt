# =====================================================================
# 1. CORE TELEGRAM GATEWAY PIPELINES
# =====================================================================
Telethon>=1.33.0

# =====================================================================
# 2. WEBRTC AUDIO TRANSMISSION NETWORKING
# =====================================================================
# Use pytgcalls with compatible tgcalls version
pytgcalls==2.1.0
tgcalls==2.0.0

# =====================================================================
# 3. STORAGE DATABASES ECOSYSYTEM LINKS
# =====================================================================
# Native multi-thread gateway for MongoDB Atlas
pymongo>=4.6.0
# Core DNS connection handshake tools for MongoDB +srv endpoints
dnspython>=2.4.0

# =====================================================================
# 4. WEB SERVER & API ENDPOINTS (REQUIRED FOR MAIN_BOT.PY)
# =====================================================================
# Missing in old list but strictly required for API execution
fastapi>=0.109.0
pydantic>=2.5.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# =====================================================================
# 5. NETWORKING & PROXIES FRAMEWORK
# =====================================================================
# SOCKS5 structural resolution layers handling outbound traffic
pysocks>=1.7.1
# Asynchronous secure request libraries
aiohttp>=3.9.0
certifi>=2024.2.2

# =====================================================================
# 6. MEDIA TRANSMISSION INTEGRATION COEFFICIENTS
# =====================================================================
cryptography>=41.0.0
PyJWT>=2.8.0
